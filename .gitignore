node_modules/
.env
.env.local
.env.audit
*.audit
.claude/skills/*/.env
*.log
.DS_Store
dist/
coverage/
.vscode/

# Dados reais de cliente (leads/contatos) — nunca versionar.
docs/inbox/*.csv
docs/inbox/*.xlsx
docs/inbox/*.xls
docs/inbox/leads-brutos/
!docs/inbox/.gitkeep

# Screenshots descartados na raiz
CleanShot*.webp
CleanShot*.png

# Snapshot de tráfego preenchido pelo secretário (dados reais; verdade fica no Supabase)
scripts/snapshot-trafego.json
