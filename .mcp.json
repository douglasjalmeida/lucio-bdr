{
  "_comment": "MCPs do projeto Lúcio (Modo Dev). Princípio de menor privilégio. supabase em --read-only por default — pra escrever, setar SUPABASE_MCP_READONLY=0 no .env, /mcp reconnect, aplicar, e voltar pra 1. Wrapper carrega .env do projeto, então não precisa exportar vars no shell antes de iniciar o Claude Code.",
  "mcpServers": {
    "n8n-luminus": {
      "command": ".claude/skills/n8n-architect/scripts/run-n8n-mcp.sh",
      "args": ["luminus"]
    },
    "supabase": {
      "command": "scripts/run-supabase-mcp.sh"
    }
  }
}
